spring:
  config:
    activate:
      on-profile: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://urlib-db.c1aekykasm7t.ap-northeast-2.rds.amazonaws.com:3306/urlib?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: 5orbmQQCs0VcaypJ2bVR
  jpa:
    open-in-view: true
    show-sql: true #
    generate-ddl: true
    hibernate:
#      ddl-auto: update
      ddl-auto: create-drop
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
  data:
    redis:
      lettuce:
        pool:
          max-active: 5
          max-idle: 5
          min-idle: 2
      host: 127.0.0.1
      port: 6379
      password: admin

  security:
    jwt:
      access-token-expiry: 600 # 10 minutes
      refresh-token-expiry: 3600 # 1 hour
      secret-key: E3s6pVxUt38WQd1a9lpyLZJOtaUATWWre5bNshmsyrQ
    oauth2:
      client:
        registration:
          kakao:
            client-id: b7449715cc40e53d10a3079c1943040e # rest api 키
            client-secret: Vm6VnCIka5OvRrwt9IXZu9HcB7eGctPz # client secret 키
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            scope:
              - account_email
              - profile_image
              - profile_nickname
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

logging:
  level:
    root: info
